# {{ project_name }} — 实验任务书

> 由 Chat2Work 从课程群聊记录中蒸馏生成
> 生成时间：{{ generated_at }}
> 源消息数：{{ total_messages }}

---

## 一、任务概述

{% if tasks %}
{% for task in tasks %}
### {{ loop.index }}. {{ task.name }}

{{ task.description | default('（描述待补充）') }}

{% if task.src_msgs %}> 溯源：{{ task.src_msgs | join(', ') }}{% endif %}

{% endfor %}
{% else %}
任务信息未能从聊天记录中自动提取，请参考原始聊天记录或询问老师。
{% endif %}

---

## 二、Deadline

{% if deadline %}
- **最终提交时间**：{{ deadline }}
{% if deadline_uncertain %}- ⚠️ 此时间为推测，未在聊天记录中明确，请向老师核实
{% if deadline_note %}- {{ deadline_note }}{% endif %}
{% elif days_left is not none and days_left >= 0 %}- **剩余时间**：{{ days_left }} 天
{% elif days_left is not none %}- ⚠️ 已过期 {{ -days_left }} 天{% endif %}
{% else %}
- ⚠️ 未从聊天记录中识别到明确 deadline，请向老师确认
{% endif %}

---

## 三、提交要求

{% if submission %}
- **提交方式**：{{ submission.method | default('未明确') }}
- **提交格式**：{{ submission.format | default('未明确') }}
- **提交截止**：{{ submission.deadline | default(deadline) }}
{% if submission.src_msgs %}> 溯源：{{ submission.src_msgs | join(', ') }}{% endif %}
{% else %}
提交方式未在聊天记录中明确，请向老师确认。
{% endif %}

---

## 四、参考资料

### 4.1 链接资源

详见 `02_参考资料/链接库.md`，共 {{ refs | length }} 条。

### 4.2 原始资料包

老师发送的压缩包/附件已解压至 `02_参考资料/原始资料包/`，原样保留。

---

## 五、注意事项

- 本任务书由 AI 从群聊记录蒸馏生成，关键信息（deadline、提交方式、评分标准）**务必与老师原始通知核对**
- 任何 `[src:msg#xxx]` 标签可在 `_provenance/msg-to-file.json` 中反查到原始消息
- 若发现信息有误或遗漏，建议直接在群里 @ 老师确认，或检查原始聊天记录

---

*任务书完*
