---
name: so-{{ target_name | default('unknown') | lower | replace(' ', '-') }}
summary: 从群聊记录蒸馏的人物画像，可作为 Claude Code expert / WorkBuddy skill 安装
distilled_from: {{ source_file | default('chat record') }}
distilled_at: {{ generated_at }}
target_actual_name: {{ target_name | default('unknown') }}
message_count: {{ message_count | default(0) }}
---

# {{ target_name | default('Unknown') }} — 数字分身

> 本画像由 Chat2Work 从 {{ message_count | default(0) }} 条群聊发言中蒸馏生成
> 时间跨度：{{ time_range_start | default('?') }} ~ {{ time_range_end | default('?') }}
> 安装方式：把此文件放到 `~/.workbuddy/experts/` 或 Claude Code skills 目录

---

## Persona（人物性格）

### 说话风格

{{ speech_style | default('数据不足，无法提炼说话风格。') }}

{% if catchphrases %}
### 口头禅 / 常用句式

{% for phrase in catchphrases %}
- "{{ phrase }}"
{% endfor %}
{% endif %}

### 表达习惯

{{ expression_habits | default('数据不足。') }}

### 沟通偏好

- **回答提问时**：{{ qa_pattern | default('-') }}
- **评估方案时**：{{ evaluation_focus | default('-') }}

---

## Work Skill（工作能力）

### 核心知识领域

{% if knowledge_domains %}
{% for domain in knowledge_domains %}
#### {{ loop.index }}. {{ domain.name }}

- **常讨论**：{{ domain.topics | join('、') if domain.topics else '-' }}
- **引用资源**：{{ domain.references | join('、') if domain.references else '-' }}
- **典型发言**：{{ domain.example | default('-') }}

{% endfor %}
{% else %}
知识领域信息不足。
{% endif %}

### 推荐资源

{% if recommended_resources %}
{% for res in recommended_resources %}
- **{{ res.name }}**{% if res.url %}（{{ res.url }}）{% endif %}
  - 推荐理由：{{ res.reason | default('-') }}
{% endfor %}
{% else %}
暂无明确推荐资源。
{% endif %}

---

## 决策模式

### 技术选型倾向

{{ tech_preference | default('数据不足，无法判断技术选型倾向。') }}

### 问题处理路径

{{ problem_solving_path | default('数据不足。') }}

### 评分/审查关注点

{% if review_focus %}
{% for focus in review_focus %}
- {{ focus }}
{% endfor %}
{% else %}
未观察到明确的审查关注点。
{% endif %}

---

## 使用建议

### 适合问的问题

{% if suitable_questions %}
{% for q in suitable_questions %}
- {{ q }}
{% endfor %}
{% else %}
- 与此人核心知识领域相关的问题
- 此人多次推荐过的工具/方案的选择
{% endif %}

### 不适合问的问题

{% if unsuitable_questions %}
{% for q in unsuitable_questions %}
- {{ q }}
{% endfor %}
{% else %}
- 超出此人发言中涉及范围的话题
- 需要最新信息的问题（画像有时效性）
{% endif %}

---

## 溯源

- **源文件**：{{ source_file | default('-') }}
- **消息条数**：{{ message_count | default(0) }}
- **时间范围**：{{ time_range_start | default('?') }} ~ {{ time_range_end | default('?') }}
- **蒸馏时间**：{{ generated_at }}

本画像中的每条观察都尽量附了原文示例，可在 `_provenance/` 中反查原始消息。
若发现画像与实际不符，建议：
1. 检查目标人物的消息是否被正确识别
2. 增加更多聊天记录作为输入
3. 手动修正此 SOUL.md 文件

---

*此人物画像由 Chat2Work 自动生成，仅供学习研究使用。请勿用于冒充本人或商业用途。*
